# 表示する画像が入っているフォルダ名を指定します。
# このフォルダは slideshow.py と同じ階層に配置してください。
PHOTO_DIR="images"

# 画像を切り替える間隔を秒単位で指定します。
# 例：30分 = 1800
INTERVAL_SECONDS=1800

# （オプション）日付表示に使用するフォントのフルパスを指定します。
# 未設定の場合は、"/usr/share/fonts/truetype/dejavu/DejaVuSans-Bold.ttf" が使用されます。
# FONT_PATH="/path/to/your/custom-font.ttf"
